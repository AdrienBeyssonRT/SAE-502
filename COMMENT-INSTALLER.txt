═══════════════════════════════════════════════════════════════
  GUIDE SIMPLE : COMMENT INSTALLER LE PROJET
═══════════════════════════════════════════════════════════════

📌 IMPORTANT : Ce projet doit être installé sur une MACHINE LINUX
   (Ubuntu, Debian, ou WSL2 sur Windows)


═══════════════════════════════════════════════════════════════
  ÉTAPE PAR ÉTAPE
═══════════════════════════════════════════════════════════════


1️⃣  TRANSFÉRER LE PROJET SUR VOTRE MACHINE LINUX
   
   Si vous êtes sur Windows et avez une VM Linux :
   - Copiez tout le dossier "SAE502 final" sur votre machine Linux
   - Utilisez WinSCP, FileZilla, ou la commande scp
   
   Si vous êtes déjà sur Linux :
   - Allez dans le dossier du projet :
     cd "SAE502 final"


2️⃣  INSTALLER ANSIBLE
   
   Ouvrez un terminal sur votre machine Linux et tapez :
   
   sudo apt update
   sudo apt install -y ansible python3-pip
   
   Vérifiez que ça marche :
   ansible --version


3️⃣  INSTALLER DOCKER (automatiquement)
   
   Dans le terminal, allez dans le projet :
   cd "SAE502 final"
   
   Lancez cette commande :
   ansible-playbook ansible/playbooks/install.yml
   
   ⏱️  Attendez 2-5 minutes
   
   ✅ Vérifiez que Docker est installé :
   docker --version


4️⃣  DÉPLOYER TOUT LE PROJET
   
   Toujours dans le dossier du projet, lancez :
   ansible-playbook ansible/playbooks/deploy.yml
   
   ⏱️  Attendez 5-10 minutes (première fois)
   
   Cette commande va :
   - Construire 4 conteneurs Docker
   - Configurer le pare-feu
   - Lancer tous les services
   
   ✅ Vérifiez que tout fonctionne :
   docker ps
   
   Vous devriez voir 4 conteneurs : firewall, logcollector, supervision, client


5️⃣  OUVRIR LA SUPERVISION
   
   Ouvrez votre navigateur web et allez sur :
   
   http://localhost:5000
   
   Vous devriez voir un tableau de bord avec les logs du pare-feu !


═══════════════════════════════════════════════════════════════
  RÉSUMÉ DES COMMANDES
═══════════════════════════════════════════════════════════════

# 1. Installer Ansible (une seule fois)
sudo apt update
sudo apt install -y ansible python3-pip

# 2. Aller dans le projet
cd "SAE502 final"

# 3. Installer Docker
ansible-playbook ansible/playbooks/install.yml

# 4. Déployer tout
ansible-playbook ansible/playbooks/deploy.yml

# 5. Ouvrir http://localhost:5000 dans votre navigateur


═══════════════════════════════════════════════════════════════
  SI ÇA NE MARCHE PAS
═══════════════════════════════════════════════════════════════

❌ Erreur "Permission denied" avec Docker ?
   → Tapez : sudo usermod -aG docker $USER
   → Déconnectez-vous et reconnectez-vous

❌ Les conteneurs ne démarrent pas ?
   → Tapez : docker-compose logs
   → Regardez les erreurs affichées

❌ Le port 5000 ne fonctionne pas ?
   → Vérifiez : docker ps | grep supervision
   → Si rien n'apparaît, relancez deploy.yml

❌ Ansible ne trouve pas les fichiers ?
   → Vérifiez que vous êtes dans le bon dossier :
     pwd
   → Vous devez être dans "SAE502 final"


═══════════════════════════════════════════════════════════════
  TESTER LE PAR-FEU
═══════════════════════════════════════════════════════════════

# Entrer dans le conteneur client
docker exec -it client bash

# Tester le pare-feu
nmap -p 22,80,443,445 firewall
ping firewall
curl http://firewall:80

# Sortir
exit


═══════════════════════════════════════════════════════════════
  VOIR LES LOGS
═══════════════════════════════════════════════════════════════

# Tous les logs
docker-compose logs -f

# Logs du pare-feu seulement
docker-compose logs -f firewall


═══════════════════════════════════════════════════════════════
  ARRÊTER TOUT
═══════════════════════════════════════════════════════════════

docker-compose down


═══════════════════════════════════════════════════════════════
  BESOIN D'AIDE ?
═══════════════════════════════════════════════════════════════

Consultez le fichier INSTALLATION.md pour plus de détails !

